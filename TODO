* frames (groups of transforms and chains) with parser;
* analytical specification of derivatives for rotation and translation
* rotation matrices with constant angular velocity (and analytical
  derivatives)
